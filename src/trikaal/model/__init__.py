"""AR backbone (Kronos_small parity) + shared from-scratch transformer primitives.

The research-surface modules (attention/blocks/model/tokenizer) are written from scratch per
the project contract — no third-party transformer implementations are imported.
"""
