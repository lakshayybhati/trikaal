% =============================================================================================
% SUBMISSION TARGET. Compile THIS, not main.tex, for anything that leaves the project.
%
%     tectonic -X compile submission.tex
%
% The only difference from main.tex is that Appendix F -- the internal self-audit, addressed to
% the integration reviewer rather than to a referee -- is omitted. main.tex remains the
% read-through build and keeps it. check_submission_build.py asserts both directions.
% =============================================================================================
\def\SUBMISSIONBUILD{1}
\input{main.tex}
