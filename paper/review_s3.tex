% =============================================================================================
% STANDALONE EXPORT OF §3 FOR EXTERNAL COLD REVIEW.
%
% Contains §3 exactly as it stands in main.tex, plus Appendices A-C, which are load-bearing for
% the section's argument (per-dimension eviction table; token-space plant construction; the two
% legibility-gate defects). Section and appendix numbering, cross-references, figures, tables and
% citations are all live — nothing is re-typed for this export, so it cannot drift from the
% manuscript.
%
% Build:  tectonic -X compile paper/review_s3.tex
% =============================================================================================
\documentclass[10pt]{article}
\usepackage{preprint}

\title{\normalsize\textsc{Section 3 --- export for review}\\[0.6em]
       \Large Capacity Eviction in Reconstruction-Trained Tokenizers}
\author{}
\date{}

\begin{document}

% §3.8 forward-references the manuscript's Limitations section, which is outside this export.
% Bind the label to its manuscript number so the reference resolves to what it resolves to there.
\makeatletter
\@namedef{r@sec:limitations}{{7}{1}{Limitations}{section.7}{}}
\@namedef{r@sec:limitations@cref}{{[section][7][]7}{1}}
\makeatother

\setcounter{section}{2}   % so the section numbers below match the manuscript
\setcounter{figure}{1}    % Figure 1 (architecture) lives in §1 and is not part of this export
\setcounter{table}{0}

{\centering
{\normalsize\textsc{Section 3 --- export for external review}}\\[0.5em]
{\Large\bfseries Capacity Eviction in Reconstruction-Trained Tokenizers}\\[0.4em]
{\small\color{tolgrey} Draft of \today\ --- section text frozen pending review}\\[1.6em]
}

\noindent\textbf{Note to the reviewer.} This is \S3 of a paper in preparation, exported verbatim
from the manuscript together with the three appendices it depends on. Numbering matches the
manuscript: \S3.1--\S3.8 and Appendices A--C. Figure~1 (the architecture schematic) belongs to \S1
and is not reproduced here; \cref{fig:eviction} is final in content but has one further production
pass pending, and \cref{fig:extraction} is a \emph{placeholder} standing in for a figure that has
been specified but not yet built --- its caption states what that figure will show. Everything
else, prose and numbers, is as intended for submission.

\medskip
\noindent\textbf{What we would most like challenged.} (i) Whether the boxed Claim in \S3.1 is
stated at a defensible scope --- as a property of reconstruction objectives in general, or of this
tokenizer class under this loss; the evidence is synthetic-fixture evidence and we may have
over-reached. (ii) Whether \S3.2's fixture genuinely isolates the single property it claims to
isolate. (iii) Whether the token-space control in \S3.4 licenses the localization conclusion, or
whether a confound survives it. (iv) Any number that does not follow from what is stated.

\bigskip
\hrule
\bigskip

\input{sections/03_mechanism}

\clearpage
\appendix
\input{sections/appendix_mechanism}

\clearpage
\bibliographystyle{plainnat}
\bibliography{refs}

\end{document}
