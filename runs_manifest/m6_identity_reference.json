{
  "compared_keys": [
    "image",
    "git_commit",
    "steps_stage1",
    "steps_stage2",
    "lockfile_sha256",
    "gpu_name",
    "cuda_build",
    "driver_version",
    "torch",
    "numpy",
    "python",
    "platform_abi",
    "attention_mode",
    "deterministic_algorithms",
    "cudnn_deterministic",
    "cudnn_benchmark"
  ],
  "platform_is_recorded_never_compared": "Linux-5.15.0-52-generic-x86_64-with-glibc2.35",
  "provenance": {
    "attention_mode": "sdpa_deterministic",
    "cuda_build": "13.0",
    "cudnn_benchmark": false,
    "cudnn_deterministic": false,
    "deterministic_algorithms": false,
    "device": "cuda",
    "driver_version": "590.48.01",
    "git_commit": "448e4fe407fa10ef7125ec3cf15872f2e3946181",
    "gpu_name": "NVIDIA GeForce RTX 4090",
    "image": "pytorch/pytorch:2.5.1-cuda12.4-cudnn9-devel",
    "lockfile_sha256": "bd1297e1ba67afafc5c3edb3559df2fcdd17f42f1b715b914c3aa69f0fe2dff4",
    "numpy": "2.4.6",
    "platform": "Linux-5.15.0-52-generic-x86_64-with-glibc2.35",
    "platform_abi": "x86_64-with-glibc2.35",
    "python": "3.11.10",
    "python_executable": "/opt/conda/bin/python3",
    "steps_stage1": 26003,
    "steps_stage2": 26003,
    "torch": "2.12.1+cu130"
  },
  "receipt": "m6_identity_reference",
  "what": "the instrument all 25 units must match. Compared by the driver's PRE-FLIGHT gate before any compute.",
  "\u2605 LIMITATION, STATED": "AT PRE-FLIGHT, deterministic_algorithms and cudnn_deterministic are False on EVERY box by construction (the orchestrator has not run yet), so 2 of the 16 keys carry NO discriminating power HERE \u2014 the same shape as 'a placeholder is identical on every shard'. They still discriminate at ASSEMBLY, where verdict.provenance_failures compares ARTIFACT stamps that were taken after determinism was forced. The pre-flight gate's power comes from the other 14: driver_version, gpu_name, image, lockfile_sha256, torch, numpy, python, platform_abi, cuda_build, attention_mode, cudnn_benchmark, git_commit and the two step budgets.",
  "\u2605 STAMPED AT THE PRE-FLIGHT CALL SITE, DELIBERATELY": "run_provenance is called TWICE per unit at DIFFERENT points: once by preflight_provenance BEFORE the orchestrator forces determinism, and once at artifact-write time AFTER. The first reference file was built from a POST-set_determinism stamp and the gate REFUSED BOTH BOXES on deterministic_algorithms/cudnn_deterministic \u2014 correctly, since it was comparing a pre-flight stamp to a post-determinism reference. Cost: nothing, refused before compute. This file is the PRE-FLIGHT stamp, so the comparison is like-for-like."
}
